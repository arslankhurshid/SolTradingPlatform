@page
@model IndexModel
@{
    ViewData["Title"] = "OData Product Service";
}

<style>
    #endpoints tr:nth-child(even) {
        background-color: #f2f2f2;
    }

   #endpoints tr {
        height: 40px; 
    }

   #endpoints td, th {
        padding: 8px 12px; 
        vertical-align: middle;
    }

    #objectDiagram {
        border-collapse: collapse; 
        margin-bottom: 20px; 
        width: 150px; 
        text-align: left;
    }

    #objectDiagram th {
        border: 1px solid black; 
        background-color: #f2f2f2; 
        font-weight: bold; 
        text-align: center;
	}

	#objectDiagram tbody {
         border: 1px solid black; 
         padding: 4px;
	}

	#objectDiagram td {
		padding: 4px;
	}

</style>

<div>
    <h1 class="display-4">OData Product Service</h1>
    <p>Dieses Service stellt eine Produktliste über eine OData-API zur Verfügung. Es unterstützt Filterung, Sortierung, Projektion und Paging nach dem OData-Standard.</p>
    <br />
    <p><strong>Base-URL:</strong> <code>http://localhost:5180</code></p>
    <br />
    <h2>Verfügbare Endpunkte</h2>
    <table id="endpoints">
        <thead>
            <tr style="background-color: #f2f2f2;">
                <th>Methode</th>
                <th>Pfad</th>
                <th style="width:60%">Beschreibung</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products</code></td>
                <td>Gibt alle Produkte zurück. Unterstützt OData-Abfrageparameter wie <code>$filter</code>, <code>$orderby</code>, <code>$top</code>, <code>$select</code>, <code>$count</code> usw.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products(1)</code></td>
                <td>Gibt das Produkt mit der ID 1 zurück.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products?$filter=Price gt 5</code></td>
                <td>Filtert Produkte mit Preis &gt; 5 EUR.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products?$orderby=Name desc</code></td>
                <td>Sortiert die Produktliste absteigend nach Name.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products?$select=Name,Price</code></td>
                <td>Gibt nur die Felder <code>Name</code> und <code>Price</code> für jedes Produkt zurück.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products?$top=2</code></td>
                <td>Gibt die ersten beiden Produkte zurück.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products?$skip=1</code></td>
                <td>Überspringt das erste Produkt und gibt den Rest zurück.</td>
            </tr>
            <tr>
                <td>GET</td>
                <td><code>/odata/Products/$count</code></td>
                <td>Gibt die Anzahl der Produkte zurück (Anzahl als Zahl).</td>
            </tr>
        </tbody>
    </table>
    <hr />
    <table id="objectDiagram">
        <thead>
            <tr>
                <th>Product</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Id: int</td>
            </tr>
            <tr>
                <td>Name: string</td>
            </tr>
            <tr>
                <td>Price: double</td>
            </tr>
        </tbody>
    </table>
</div>
